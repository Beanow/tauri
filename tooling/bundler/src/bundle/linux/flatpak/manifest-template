{
    "app-id": "{{app_id}}",
    "runtime": "org.gnome.Platform",
    "runtime-version": "42",
    "sdk": "org.gnome.Sdk",
    "command": "{{main_binary}}",
    "finish-args": [
        "--share=ipc",
        "--socket=x11",
        "--socket=wayland",
        "--socket=pulseaudio",
        "--device=dri",
        "--env=LD_LIBRARY_PATH=/app/lib",
        "--talk-name=org.freedesktop.Notifications",
        "--talk-name=org.kde.StatusNotifierWatcher",
        "--talk-name=com.canonical.AppMenu.Registrar",
        "--talk-name=com.canonical.indicator.application",
        "--talk-name=com.canonical.Unity.LauncherEntry"
    ],
    "rename-desktop-file": "{{main_binary}}.desktop",
    "rename-icon": "{{main_binary}}",
    "modules": [
        "shared-modules/libappindicator/libappindicator-gtk3-12.10.json",
        {
            "name": "{{app_id}}",
            "buildsystem": "simple",
            "sources": [
                {
                    "type": "file",
                    "dest-filename": "{{main_binary}}.deb",
                    "path": "{{project_out_directory}}/bundle/deb/{{deb_package_name}}.deb"
                }
            ],
            "build-commands": [
                "ar x {{main_binary}}.deb",
                "tar xf data.tar.{x,g}z deb_data",
                "cp -r deb_data/usr/* /app"
            ]
        }
    ]
}